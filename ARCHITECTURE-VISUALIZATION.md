# Architecture Visualization - E-Commerce Platform

## 🏗️ Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          USER INTERFACE LAYER                            │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    React Frontend (Port 3000)                     │   │
│  │                                                                   │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │   │
│  │  │Dashboard │  │ Products │  │  Orders  │  │  Users   │        │   │
│  │  │   Page   │  │   Page   │  │   Page   │  │   Page   │        │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │   │
│  │                                                                   │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │              API Service Layer (Axios)                    │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTP/REST
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        GATEWAY & ROUTING LAYER                           │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │              API Gateway (Port 8080)                            │   │
│  │                                                                   │   │
│  │  • Route Management           • Circuit Breakers                │   │
│  │  • Load Balancing            • Resilience4j                     │   │
│  │  • Service Discovery         • Fallback Mechanisms              │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
                    ▼               ▼               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      SERVICE DISCOVERY LAYER                             │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │           Eureka Server (Port 8761)                             │   │
│  │                                                                   │   │
│  │  • Service Registration       • Health Monitoring                │   │
│  │  • Service Discovery         • Load Balancing Info              │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     CONFIGURATION LAYER                                  │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │           Config Server (Port 8888)                             │   │
│  │                                                                   │   │
│  │  • Centralized Configuration                                    │   │
│  │  • Property Management                                          │   │
│  │  • Environment-specific Settings                               │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      MICROSERVICES LAYER                                 │
│                                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │ User Service │  │Product Service│  │Order Service │  │  Payment   │ │
│  │  Port 8083   │  │  Port 8081   │  │  Port 8085   │  │  Service   │ │
│  │              │  │              │  │              │  │ Port 8084  │ │
│  │ • User CRUD  │  │ • Product    │  │ • Order      │  │            │ │
│  │ • Roles      │  │   Catalog    │  │   Creation   │  │ • PayPal   │ │
│  │   (CLIENT,   │  │ • Categories │  │ • Stock Mgmt │  │   Integration│
│  │    ADMIN)    │  │ • Inventory  │  │ • Status     │  │ • Payment  │ │
│  │              │  │              │  │   Tracking   │  │   Tracking │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └────────────┘ │
│         │                  │                  │                  │      │
│         │                  │                  │                  │      │
│         │    OpenFeign     │    OpenFeign     │                  │      │
│         │   <────────────> │   <────────────> │                  │      │
│         │                  │                  │                  │      │
└─────────┼──────────────────┼──────────────────┼──────────────────┼──────┘
          │                  │                  │                  │
          ▼                  ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        DATABASE LAYER                                    │
│                                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │   H2 DB      │  │   H2 DB      │  │   H2 DB      │  │   H2 DB    │ │
│  │  user_db     │  │  product_db  │  │  order_db    │  │ payment_db │ │
│  │              │  │              │  │              │  │            │ │
│  │ • Users      │  │ • Products   │  │ • Orders     │  │ • Payments │ │
│  │   Table      │  │   Table      │  │   Table      │  │   Table    │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      EXTERNAL INTEGRATIONS                               │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                 PayPal Sandbox API                              │   │
│  │                                                                   │   │
│  │  • Payment Processing         • Transaction Tracking            │   │
│  │  • Order Approval            • Sandbox Environment              │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow Example: Creating an Order

```
1. User clicks "Create Order" in React Frontend
   │
   ▼
2. Frontend calls POST /api/orders via Axios
   │
   ▼
3. Request arrives at API Gateway (Port 8080)
   │
   ├─► Circuit Breaker checks if Order Service is healthy
   │
   ▼
4. Gateway routes to Order Service (Port 8085)
   │
   ▼
5. Order Service receives request
   │
   ├─► Validates order data
   │
   ├─► Uses OpenFeign to call Product Service
   │   │
   │   ▼
   │   Product Service (Port 8081)
   │   │
   │   ├─► Checks product exists
   │   ├─► Verifies stock availability
   │   ├─► Reduces stock quantity
   │   ├─► Updates H2 product_db
   │   │
   │   ▼
   │   Returns success to Order Service
   │
   ├─► Creates order record
   ├─► Saves to H2 order_db
   │
   ▼
6. Order Service returns success to Gateway
   │
   ▼
7. Gateway returns response to Frontend
   │
   ▼
8. Frontend updates UI with new order
   │
   └─► Shows success message
```

---

## 🛡️ Circuit Breaker Flow

```
Normal Flow:
Frontend → Gateway → Service → Database → Response

Service Down:
Frontend → Gateway → [Service DOWN] → Circuit Open
                  ↓
           Fallback Response → "Service Unavailable"
                  ↓
           Frontend shows error message
```

---

## 📊 Port Mapping Summary

| Component | Port | Protocol | Purpose |
|-----------|------|----------|---------|
| React Frontend | 3000 | HTTP | User Interface |
| API Gateway | 8080 | HTTP | Entry Point & Routing |
| Product Service | 8081 | HTTP | Product Management |
| User Service | 8083 | HTTP | User Management |
| Payment Service | 8084 | HTTP | Payment Processing |
| Order Service | 8085 | HTTP | Order Processing |
| Eureka Server | 8761 | HTTP | Service Discovery |
| Config Server | 8888 | HTTP | Configuration |

---

## 🔌 Inter-Service Communication Patterns

### 1. Synchronous (REST via OpenFeign)
```
Order Service ──[OpenFeign]──> Product Service
    │
    └─► Check Stock
    └─► Reduce Stock
    └─► Restore Stock (on cancel)
```

### 2. Service Discovery
```
All Services ──[Register]──> Eureka Server
                                 │
Gateway ──[Discover]──> Eureka Server
                                 │
                          Get Service Locations
```

### 3. Configuration
```
All Services ──[Bootstrap]──> Config Server
                                  │
                           Load Properties
```

---

## 🎯 Data Flow: Complete User Journey

```
1. USER VISITS WEBSITE
   └─► React App loads at localhost:3000
       └─► Dashboard displays architecture

2. USER VIEWS PRODUCTS
   └─► Click "Products" in navigation
       └─► Frontend: GET /api/products
           └─► Gateway routes to Product Service
               └─► Product Service queries H2 product_db
                   └─► Returns product list with stock
                       └─► Frontend displays in grid

3. USER FILTERS BY CATEGORY
   └─► Click "ELECTRONICS" category
       └─► Frontend: GET /api/products/category/ELECTRONICS
           └─► Product Service filters results
               └─► Returns filtered products
                   └─► Frontend updates display

4. USER CREATES ORDER
   └─► Click "Create Order" on Orders page
       └─► Fill form (User ID, Product, Quantity)
           └─► Frontend: POST /api/orders
               └─► Gateway validates & routes
                   └─► Order Service receives request
                       ├─► Calls Product Service (check stock)
                       ├─► Product Service reduces stock
                       ├─► Order Service creates order
                       └─► Both update their H2 databases
                           └─► Frontend receives success
                               └─► Updates UI, shows order

5. USER CANCELS ORDER
   └─► Click "Cancel Order" button
       └─► Frontend: PATCH /api/orders/{id}/cancel
           └─► Order Service receives cancellation
               ├─► Calls Product Service (restore stock)
               ├─► Updates order status to CANCELLED
               └─► Both update databases
                   └─► Frontend refreshes order list
```

---

## 🔐 Security Layers (Future Enhancement)

```
┌─────────────────────────────────────────┐
│ Future: JWT Authentication              │
├─────────────────────────────────────────┤
│ Current: Open API (Academic Project)    │
└─────────────────────────────────────────┘
```

---

## 📈 Scalability Design

```
Load Balancer (Future)
    │
    ├─► API Gateway Instance 1
    ├─► API Gateway Instance 2
    └─► API Gateway Instance 3
            │
            ├─► Service Instance 1
            ├─► Service Instance 2
            └─► Service Instance 3

Eureka handles discovery of all instances
Gateway provides client-side load balancing
```

---

## 🎨 Technology Stack Layers

```
┌──────────────────────────────────────┐
│   FRONTEND LAYER                     │
│   React 19 + React Router + Axios    │
└──────────────────────────────────────┘
               ▼
┌──────────────────────────────────────┐
│   GATEWAY LAYER                      │
│   Spring Cloud Gateway + Resilience4j│
└──────────────────────────────────────┘
               ▼
┌──────────────────────────────────────┐
│   SERVICE LAYER                      │
│   Spring Boot 3.4.1 + OpenFeign      │
└──────────────────────────────────────┘
               ▼
┌──────────────────────────────────────┐
│   DATA LAYER                         │
│   Spring Data JPA + H2 Database      │
└──────────────────────────────────────┘
               ▼
┌──────────────────────────────────────┐
│   EXTERNAL LAYER                     │
│   PayPal API (Sandbox)               │
└──────────────────────────────────────┘
```

---

## 🎯 Key Architectural Patterns Implemented

1. **Microservices Architecture** ✅
   - Independent, loosely coupled services
   - Single responsibility per service

2. **API Gateway Pattern** ✅
   - Single entry point
   - Request routing and composition

3. **Service Discovery** ✅
   - Dynamic service registration
   - Client-side service discovery

4. **Circuit Breaker Pattern** ✅
   - Fault tolerance
   - Graceful degradation

5. **Database per Service** ✅
   - Data isolation
   - Independent scaling

6. **Centralized Configuration** ✅
   - Config Server
   - Environment-specific settings

7. **Client-Side Communication** ✅
   - OpenFeign for inter-service calls
   - Declarative REST clients

---

This architecture demonstrates enterprise-level microservices design patterns suitable for production environments with proper scaling and resilience mechanisms.

