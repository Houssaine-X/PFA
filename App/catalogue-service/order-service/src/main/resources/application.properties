# Order Service - Local Configuration
# All main configurations are managed by Config Server (http://localhost:8888)
# This file contains ONLY local fallback properties

# NOTE: Service name and Config Server connection are in bootstrap.properties
# All other configurations (DB, Eureka, JPA, etc.) are fetched from Config Server

